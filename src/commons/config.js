const config = {
    // API_SERVER: process.env.NODE_ENV == 'development' ? 'http://localhost:5000/' : 'http://api.avataroad.com:50005000/'
    API_SERVER: "http://20.41.113.72:5000/" // avataroad-deeplearning-backend
}

export default config;